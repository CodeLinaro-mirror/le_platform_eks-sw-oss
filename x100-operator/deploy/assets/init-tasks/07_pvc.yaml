apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csm-x100-pvc
  namespace: x100-operator-resources
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: manual
